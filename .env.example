# Optional: local-only GPT-Live voice demo
# Never set VITE_GPT_LIVE_DEMO in production / Netlify.
# Copy to .env.local and fill the key, then: npm run dev:gpt-live
VITE_GPT_LIVE_DEMO=true
OPENAI_API_KEY=

# Optional Supabase override (solo Nuzlocke works without these)
# VITE_SUPABASE_URL=
# VITE_SUPABASE_ANON_KEY=
